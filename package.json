{
  "name": "glassnet",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["api", "web"],
  "scripts": {
    "postinstall": "npm run db:generate -w api",
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w api && npm run build -w web",
    "start": "concurrently -n api,web \"npm run start -w api\" \"npm run start -w web\"",
    "db:push": "npm run db:push -w api",
    "db:studio": "npm run db:studio -w api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
